from . import colors

# A tuple of two colors is (fg, bg)

DEFAULT = (colors.WHITE,)
ACTION_IMPOSSIBLE = (colors.GRAY,)

PLAYER_ATTACKS_NPC = (colors.WHITE,)
NPC_ATTACKS_PLAYER = (colors.RED,)
NPC_ATTACKS_NPC = (colors.WHITE,)

ENTITY_DIED = (colors.BLACK, colors.RED)